/**
 * Settings - 路由入口（add-settings-and-shell）
 *
 * 实际实现在 src/pages/settings/Settings.tsx（4 区块主入口）。
 * 旧版本是 PlaceholderPage 占位，本 change 升级为真页。
 */

import Settings from './settings/Settings';

export default Settings;
